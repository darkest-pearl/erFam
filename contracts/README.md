# Contracts

Versioned schemas will define APIs, events, call states, allocation states,
error codes, idempotency behavior, configuration, gateway telemetry, and
compatibility rules before feature implementation.

Semantic names must have one meaning across every component.
