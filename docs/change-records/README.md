# Change records

Create immutable work-package and contract-change records here from the
templates in `docs/templates`. The filename begins with its immutable ID.

Records are append-oriented. Corrections should preserve the original decision,
identify the superseding record, and remain attributable.
