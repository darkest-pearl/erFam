# Control service

Authoritative identity, device, configuration, destination-policy, allocation,
ledger, fairness, call-state, retention, and audit behavior.

This service owns the canonical state reducer and must remain deterministic
under retries and concurrent requests.
