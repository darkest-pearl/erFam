# Home-edge gateway

Runs the controlled PBX and line-driver boundary near the physical carrier
device. The real adapter is developed and isolated here, using the reviewed
SIP/PJSIP and telemetry interfaces.

The demo route is the default. The live route must be disabled unless every
authorization and activation gate is satisfied.
