# Media-control service

Coordinates authenticated calling sessions and reconnection without becoming a
source of quota or policy truth. It must enforce the ten-second default
reconnection window and consume versioned control contracts.
